# /etc/xroad/conf.d/local.ini on cs.gerege.mn — values redacted
# Real values live on the server only. See reference_cs_secrets.md in the
# operator's local memory for retrieval (ssh + sudo).
[management-service]
api-token = REDACTED

[registration-service]
api-token = REDACTED

[center]
backup-encryption-enabled = true
backup-encryption-keyids = REDACTED
